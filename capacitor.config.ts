import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'dk.nielsskov.lifestylebook',
  appName: 'Lifestyle Book',
  webDir: 'dist',
}

export default config
